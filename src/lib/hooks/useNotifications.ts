/**
 * useNotifications Hook
 *
 * Re-export hook akses NotificationContext dari provider agar konsisten
 * dengan struktur hook di `src/lib/hooks`.
 */

export { useNotifications } from "@/providers/NotificationProvider";
