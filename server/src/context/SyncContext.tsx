/**
 * SyncContext
 *
 * Context for sync queue management
 * Re-exports from SyncProvider for convenience
 */

export { useSyncContext } from "@/providers/SyncProvider";
export type { UseSyncReturn } from "@/lib/hooks/useSync";
